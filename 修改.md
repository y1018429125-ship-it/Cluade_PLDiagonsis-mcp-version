# PLDiagnosis 项目修改记录

> 本文件用于记录对 PLDiagnosis 智能体项目的所有修改、优化与调整。
> 包括但不限于：系统代码、大模型调用、skills、MCP tools、提示词、配置等。

---

## 修改规范

1. 每次修改前请简要说明修改目的、影响范围和预期效果。
2. 修改完成后请记录实际结果、验证方式和遗留问题。
3. 如修改涉及 MCP tool 或 skill，请同时更新相关配置和文档。
4. 保持记录简洁、可追溯，避免过长描述。

---

## 修改记录

### 2026-07-08

#### 1. 替换 LightningDiagnosisTool 为真实雷电故障诊断 MCP 服务

- **修改目的**：将 PLD 中虚拟的雷电诊断工具替换为基于真实数据的雷电故障诊断 MCP 服务。
- **涉及范围**：
  - 新增/替换 `/Users/yfzx/Desktop/Cluade_PLDiagonsis-master/mcp-services/lightning-service/` 目录
  - 服务监听端口：`8001`
- **主要变更**：
  - `main.py` 从返回模拟数据改为调用后端 Mock API，执行 4 模块加权诊断。
  - 保留 FastAPI `/health`、`/diagnose` 接口协议，与 PLD `MCPToolAdapter` 兼容。
  - 输出包含 Markdown 报告、3 张波形图 base64、5 个模块分数、故障类型和综合置信度。
- **验证方式**：
  - `GET /health` 返回正常
  - `POST /diagnose` 对 `雅湖线 2025-05-08` 返回 `雷击-绕击`、`confidence=0.985`、5 条证据、5 个模块分数、3 张图片
  - `pytest test_main.py -v` 全部通过
- **遗留问题 / 待优化**：
  - `config.py` 中 BASE_URL、账号、密码仍硬编码，需改为环境变量配置。
  - `config/tools/lightning.yaml` 中的 `output_schema` 字段仍描述旧虚拟工具输出，建议同步更新。
  - `models.py` 中 `timestamp` 默认值为 naive datetime，建议改为 `datetime.now(timezone.utc)`。
  - 每次请求新建 `httpx.AsyncClient`，未复用连接池，高并发下可优化。

---

### 2026-07-10

#### 2. 新增 MCP 工具调用记录功能

- **修改目的**：为了便于调试 PLD 智能体，记录每次 MCP tool 实际返回的结果。
- **涉及范围**：
  - 修改 `/Users/yfzx/Desktop/Cluade_PLDiagonsis-master/src/infrastructure/adapters/mcp_adapter.py`
- **主要变更**：
  - 在 `MCPToolAdapter.execute()` 成功返回 `ToolOutput` 后，调用新增私有方法 `_save_tool_output()`。
  - `_save_tool_output()` 根据适配器配置的 URL 端口，反向查找 `mcp-services/` 下对应的服务目录名。
  - 以追加模式将调用时间、请求 payload、raw_text、structured_data、metadata 写入 `mcp-services/{service_dir}.md`。
  - 写入失败被 try/except 捕获，不影响主业务流程。
- **验证方式**：
  - `python3 -m py_compile src/infrastructure/adapters/mcp_adapter.py` 通过
  - 直接调用 `MCPToolAdapter.execute()` 成功生成 `mcp-services/lightning-service.md`
  - md 文件中包含完整请求和响应内容
- **未覆盖边界**：
  - HTTP 错误路径（`httpx.HTTPError`）不会保存调用记录。
  - 未测试其余 4 个 MCP 工具。
  - 未测试并发调用时的文件写入安全性。
- **最终判定**：
  - Pass，功能符合需求，可合并到当前工作流。
- **可以优化的点**：
  - 当前时间戳为 UTC，可考虑改为本地时间。
  - `raw_text` 含 Markdown 特殊字符时可能影响 md 文件渲染。
  - md 文件会持续增大，需定期清理或归档。
  - 端口反查依赖 `main.py` 中的硬编码端口模式，若未来通过其他方式配置端口会失效。

---

## 待办事项

- [ ] 将 `lightning-service/config.py` 硬编码配置改为环境变量读取。
- [ ] 更新 `config/tools/lightning.yaml` 的 `output_schema` 以匹配新的 `structured_data`。
- [ ] 优化 `httpx.AsyncClient` 复用机制。
- [x] ~~将 MCP 调用记录时间戳从 UTC 改为本地时间（可选）。~~ 已完成，改为北京时间。
- [ ] 处理 `raw_text` 中 Markdown 特殊字符，避免渲染异常（可选）。
- [ ] 增加 MCP 调用记录的归档或清理策略（可选）。
- [ ] 修复或替换 `WeatherDiagnosisTool`（当前通过提示词临时屏蔽）。
- [ ] 优化 `compose_report` 阶段时延（当前约 41 秒，为 LLM 生成报告时间）。

---

### 2026-07-13

#### 3. 前端端到端验证真实雷电诊断 MCP 服务

- **修改目的**：确认真实雷电诊断 MCP 服务在 PLD 完整链路中被正确调用，报告生成正常。
- **涉及范围**：
  - PLD 前端（Vue 3）+ 后端（Flask）+ `lightning-service`（端口 8001）
- **主要变更**：
  - 启动 PLD 前后端及 `lightning-service`。
  - 在前端输入诊断问题，触发完整诊断流程。
  - 验证 `MCPToolAdapter` 正确调用 `http://localhost:8001/diagnose`。
- **验证方式**：
  - 前端能收到完整 SSE 流并展示诊断结果。
  - `mcp-services/lightning-service.md` 中记录了本次调用。
- **遗留问题 / 待优化**：
  - 验证过程中发现只有 3 个 MCP 服务生成 `.md` 调用记录文件（lightning、wind、bird），属于正常行为：
    - `icing-service` 在诊断计划阶段常被智能体跳过（非当前季节/场景）。
    - `weather-service` 使用自定义浏览器代理适配器，不经过 `MCPToolAdapter`，因此无 `.md` 记录。

---

### 2026-07-17

#### 4. 新增系统日志记录前端输出与全流程时延

- **修改目的**：每次在前端提问时，记录前端完整输出以及 PLD 智能体回答问题的全流程细分时延，便于后续调试功能与性能。
- **涉及范围**：
  - 后端：`src/infrastructure/diagnosis_logger.py`（新增）
  - 后端：`src/core/models.py`（`ExecutionContext` 新增 `diagnosis_logger` 字段）
  - 后端：`src/application/commands/diagnose.py`（各阶段计时）
  - 后端：`src/domain/tool_executor.py`（单工具耗时）
  - 后端：`src/interfaces/web.py`（SSE 事件记录、前端日志合并接口 `/api/log/frontend`）
  - 前端：`web/src/api/http.ts`（新增 `sendFrontendLog`）
  - 前端：`web/src/stores/sessionStore.ts`（捕获时间线、发送前端日志）
- **主要变更**：
  - 新增 `DiagnosisLogger` 类，按日期分目录、按会话写入 JSON 日志。
  - `DiagnoseCommand` 对 10 个主要阶段记录开始/结束时间。
  - `ToolExecutor` 记录每个工具的调用耗时与成败。
  - `_chat_stream` 将每个 SSE 事件写入日志。
  - 前端在 SSE 流结束后，自动将完整 `messages` 列表和时间线 POST 到 `/api/log/frontend`。
  - 后端合并前端数据，最终文件包含 `frontend_output`、`frontend_timeline`、`stages`、`events`、`tool_timings`。
- **验证方式**：
  - `python3 -m py_compile` 通过相关后端文件。
  - 使用 Playwright 真实浏览器访问 `http://localhost:5173/` 发送诊断问题。
  - 自动生成日志文件，包含 2 条前端消息、完整时间线、10 个阶段、6 个工具耗时。
- **遗留问题 / 待优化**：
  - `thinking` 事件重复生成较多，日志文件可能偏大，可考虑采样或压缩。
  - 当前日志保存在本地 `logs/diagnosis/`，长期运行需考虑归档策略。

### 2026-07-17

#### 5. 屏蔽 WeatherDiagnosisTool 以优化诊断时延

- **修改目的**：`WeatherDiagnosisTool` 通过浏览器代理抓取百度天气，经常超时，单次诊断耗时 109 秒，严重拖慢整体响应。
- **涉及范围**：
  - 修改 `skills/comprehensive_diagnosis.md`
- **主要变更**：
  - 在工具调用策略表格中增加 `WeatherDiagnosisTool` 行，标记为 `DO NOT call`。
  - 在注意事项中增加明确说明：`Skip WeatherDiagnosisTool completely - it is currently disabled due to browser automation timeout`。
- **验证方式**：
  - 重启服务后发送诊断请求，日志显示只调用 3 个工具（Lightning、Wind、Bird）。
  - `execute_tools` 阶段从 109 秒降到约 0.4 秒。
  - 前端总诊断耗时从约 2.5 分钟降到约 52 秒。
- **遗留问题 / 待优化**：
  - 未来修复天气工具后，需要删除这两行提示词说明以恢复调用。
  - 当前 `compose_report` 阶段成为新瓶颈（约 41 秒），这是 LLM 生成报告的时间。

---

#### 6. MCP 调用记录改为覆盖模式并使用北京时间

- **修改目的**：
  1. 每次 MCP 调用记录只保留最新一次，便于查阅。
  2. 将调用时间从 UTC 改为北京时间。
- **涉及范围**：
  - 修改 `src/infrastructure/adapters/mcp_adapter.py`
- **主要变更**：
  - `_save_tool_output` 写入模式从追加 `"a"` 改为覆盖 `"w"`。
  - 时间戳从 `datetime.now(timezone.utc)` 改为 `datetime.now(timezone(timedelta(hours=8)))`。
  - 文档字符串同步更新为"覆盖写入"。
- **验证方式**：
  - `python3 -m py_compile src/infrastructure/adapters/mcp_adapter.py` 通过。
  - 调用后 `mcp-services/lightning-service.md` 从 698,855 字节降到约 100 KB。
  - `grep -c '## 调用记录' mcp-services/lightning-service.md` = 1，确认只有一条记录。
  - 时间戳显示为 `2026-07-17 15:21:21 UTC+08:00`。
- **遗留问题 / 待优化**：
  - 覆盖模式会丢失历史记录，如需恢复追加模式，需改回 `"a"`。
  - 高并发场景下多个请求可能互相覆盖，当前单用户场景可接受。

---

## 备注

- 后续对项目的任何修改，请按时间倒序追加到本文件。
