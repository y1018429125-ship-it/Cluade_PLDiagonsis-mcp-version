# PLDiagnosis 项目修改记录

> 本文件用于记录对 PLDiagnosis 智能体项目的所有修改、优化与调整。
> 包括但不限于：系统代码、大模型调用、skills、MCP tools、提示词、配置等。

---

## 修改规范

1. 每次修改前请简要说明修改目的、影响范围和预期效果。
2. 修改完成后请记录实际结果、验证方式和遗留问题。
3. 如修改涉及 MCP tool 或 skill，请同时更新相关配置和文档。
4. 保持记录简洁、可追溯，避免过长描述。

---

## 修改记录

### 2026-08-14

#### 25. 流式输出前端不生效的修复（响应式触发根因）

- **问题**：条目 24 上线后用户实测流式无效——仍是报告生成完才一次性显示。
- **根因**（四段排查）：后端 curl 实证 863 个 chunk 逐字到达 ✓；构建产物含新代码 ✓；sse.ts 不过滤事件类型 ✓ → 定位到 `sessionStore.ts`：`assistantMsg` 是普通对象（line 118），流式期间所有 mutation（eventType/report 累积/reportStreaming）都改在原始对象上，**Vue 只有经响应式代理的 set 才触发重渲染**——数据在内存里正常累积，但界面直到最后 `isLoading.value = false`（代理 set）才重渲染一次。佐证：`msg.thinking` 从未在模板中渲染，说明本项目此前从未有过流式期间的实时界面更新，该缺陷一直存在只是无感知。
- **修复**：`assistantMsg` 用 `reactive()` 包装（+ vue import），每次 chunk mutation 即触发渲染。
- **涉及范围**：`web/src/stores/sessionStore.ts`（2 处：import + reactive 包装）。
- **验证方式**：npm run build 成功（新 bundle index-DjAJENkE.js）；机制层面每次 mutation 必经代理。浏览器端效果待用户强制刷新后复测。
- **遗留问题**：无。

#### 24. 报告流式输出 + 历史关联并行预取

- **修改原因**：时延排查结论——compose_report 占诊断总时长 75-87%（快档 22-29s / 慢档 83-90s，波动源于共享 LLM 服务器负载，不可控）；报告耗时由 LLM decode 速度决定，prompt 精简无法缩短实际时长，唯一有效杠杆是流式输出（感知时延：2-3s 后开始逐字显示）。
- **设计确认**（用户讨论后确定）：
  - 消息卡片提问后即出现（现状），工具执行完立即开始流式生成报告，无需点击；"查看报告"只是查看进度。
  - 报告流式开始即触发历史关联预取（原在 complete 后），两者并行；实测并发争抢可忽略（compose 26.6s vs 单独 22-29s，KB 88.2s vs 85s——vLLM 连续批处理，2 路并发处于"搭便车区间"）。
- **涉及范围**（6 个文件，ULW 4 轮）：
  - `src/core/models.py` → `EventType.REPORT_CHUNK` + `Event.report_chunk()`（payload.content 增量）。
  - `src/domain/report_composer.py` → 新增 `compose_stream()`（复用 `_build_prompt`，调 `stream_chat`）；提取公共 `finalize_response()`（清洗+摘要），`compose()` 同步改用。
  - `src/application/commands/diagnose.py` → compose 阶段边收边发 report_chunk，收完后 finalize 入库、complete 携带最终全文（格式不变，覆盖流式累积）。
  - `src/infrastructure/diagnosis_logger.py` → report_chunk 增量合并为单条（防日志重新膨胀）。
  - `web/src/types/index.ts`、`web/src/stores/sessionStore.ts` → report_chunk 累积到 `msg.report` + `reportStreaming` 标记，complete 时清除。
  - `web/src/components/ChatPanel.vue` → 新增"正在生成诊断报告▍"渲染分支（流式期间可点"查看报告（生成中…）"实时看进度）；预取触发提前到报告流开始；流式期间面板跟随滚动。
- **验证方式**：
  - 后端 E2E：863 个 report_chunk 在 22.2s 窗口逐字推进（compose 22.3s），流式累积全文与最终报告一致；日志 863 块合并为 1 条、23.6KB 不膨胀。
  - 前端：npm run build 成功；渲染分支顺序核验（start→thinking→streaming→summary→content，complete 后正确落到 summary 卡片）。
- **遗留问题 / 待优化**：
  - 浏览器端实测（逐字渲染、并行预取体感）需用户打开页面验证。
  - modify_report（修改报告）保持非流式独立路径，未改动。
  - 流式期间报告区渲染的是未清洗文本（complete 后由 sanitize 版覆盖，可能见瞬时 LaTeX 字符，概率低）。

### 2026-08-13

#### 23. 报告 prompt 剔除 base64 图片（token 精简，非时延优化）

- **修改原因**：时延实测发现 compose_report 占诊断总时长 75-87%（28-83s，随报告长度波动），排查发现报告 prompt 中雷电工具 `details.images` 的 base64（约 90KB ≈ 2-3 万 token）被 `json.dumps` 原样传入 LLM。
- **方案选择**（用户确认方案 1）：仅在 `_build_prompt()` 组装 prompt 时剔除 images（浅拷贝替换为占位说明 `"<N 张图片，文字报告未使用，已省略>"`）；雷电 MCP 返回、SSE result 事件、会话缓存中的图片数据原样保留，为将来前端展示/报告插图留数据通路。
- **实证结论**（改变原始预期，诚实记录）：
  - 时延收益≈0：compose_report 28.7s → 28.0s（波动范围内）。证明报告耗时由 LLM decode 速度（实测约 35-55 token/s）决定，2-3 万 token prefill 仅约 0.7s——**prompt 精简无法缩短报告生成时间，唯一有效杠杆是流式输出（感知时延，用户暂未决策）**。
  - LLM 读不懂文本字段中的 base64（机制：多模态需 image_url 内容块；实证：优化前后两份报告图片引用均为 0、内容结构一致）——用户据此确认改动保留。
  - 保留价值：每次报告少传约 2-3 万 token（计费/限流场景的成本下降），prompt 更干净。
- **涉及范围**：`src/domain/report_composer.py` → `_build_prompt()` 一处。
- **验证方式**：单元断言（prompt 无 base64、占位说明存在、province/voltage 等字段完整、原始数据未污染）；端到端诊断报告内容验证（`0 × 1.0 = 0`、加权章节、章节结构均正常）。
- **遗留问题 / 待优化**：流式报告输出（`stream_chat()` 已存在于 llm_service，需改 diagnose.py + web.py + 前端渲染，约 3 文件）是剩余唯一时延优化方向，待用户决策。

#### 22. 历史关联答案随会话持久化（跨刷新/跨重启秒开）

- **修改原因**：历史关联缓存原仅存在前端页面内存，刷新页面/重开浏览器后首次点击需重新等约 85 秒；而答案由会话持久化数据派生（创建后不变），在会话生命周期内是确定的，缓存层级放错。
- **失效策略讨论结论**（用户确认）：知识库更新只发生在两次诊断之间；点过"完成诊断"的会话状态为 COMPLETED，`get_or_create()` 跳过已完成会话，下一次诊断必创建新会话 → 必重新查询知识库。故无需 TTL/版本号/刷新按钮。老会话重开时显示当时缓存的答案，与该会话报告内容保持一致，是特性而非缺陷。
- **涉及范围**（3 个文件）：
  - `src/core/models.py` → `DiagnosisSession` 新增 `history_relation_cache: Optional[List[Dict[str, str]]] = None`（随 sessions.json 持久化，旧会话文件由 pydantic 默认值兼容）。
  - `src/interfaces/web.py` → `history_relation` 开头先查缓存（命中带 `cached: true` 直接返回）；查询成功后写入 session 并 `_persist()`。
  - `src/application/commands/diagnose.py` → province/voltage 幂等写入块：仅当**新写入**字段时清空缓存（覆盖"老会话经链式重诊断补齐字段后兜底缓存过期"的唯一过期场景）。
- **验证方式**：
  - 第一次调用 83.7s 实时查询（cached: false）→ 第二次 0.008s（cached: true）。
  - `data/sessions.json` 落盘检查：2 个 section、标题与答案长度正确。
  - 重启 Flask 进程后调用 0.019s 仍命中（跨进程持久化成立，即刷新页面后秒开场景）。
  - 前端零改动，预取逻辑自动受益。
- **遗留问题 / 待优化**：并发两次首查会各查一次 KB（无锁），结果相同幂等无害；缓存随会话永久有效（经讨论确认无失效需求）。

#### 21. 知识库地址迁移到配置文件

- **修改原因**：历史关联调用的 `http://localhost:8503` 原硬编码在 `src/interfaces/web.py`，不便部署调整。
- **涉及范围**（4 个文件）：
  - `src/core/config.py` → 新增 `KnowledgeBaseConfig`（`base_url`，默认值 `http://localhost:8503`，支持 `KB_BASE_URL` 环境变量覆盖），挂到 `AppConfig.knowledge_base`。
  - `config/config.yaml` → 新增 `knowledge_base: {base_url: http://localhost:8503}` 段。
  - `src/interfaces/dependency_injection.py` → `_merge_yaml_config` 仿 session/tools 模式合并 knowledge_base 段。
  - `src/interfaces/web.py` → `kb_url` 改为 `container.config.knowledge_base.base_url.rstrip("/") + "/query"`。
- **验证方式**：`py_compile` 全过；容器加载实测 `base_url = http://localhost:8503`（yaml 合并生效）；重启 Flask 后 curl `/api/history-relation` 返回 success=True，两个 section 内容正常（一、雅湖线历年情况 / 二、江西±800kV线路2025年4-6月情况）。
- **遗留问题 / 待优化**：yaml 缺 knowledge_base 段时回退默认值 `http://localhost:8503`（与原行为一致，向后兼容）。
- **同步决策记录**（2026-08-13 用户决策，仅更新 requirements.md 不改代码）：风向 wd 暂不解决（无法判断 wd 参数与风向对应关系）；诊断日志不做定期清理（需要时手动清理）。

#### 20. 诊断日志瘦身（thinking 只记一次 + images 占位符）

- **修改原因**：单次诊断日志约 0.4MB，其中 thinking 事件占 60%（每个事件携带累计前缀文本，O(n²) 重复记录，实测单次 325 条）、result 事件中雷电 base64 图片占 38%（约 90KB）；日志无轮转无清理会无限增长（3 天实测 20MB）。
- **涉及范围**：`src/infrastructure/diagnosis_logger.py` → `record_event()` 单点修改（所有 SSE 事件记录的唯一入口，覆盖主命令与链式重诊断两条路径）：
  - thinking：连续累计前缀消息（后一条以前一条为前缀）原地合并为一条，只保留最终完整文本；状态提示类 thinking（非前缀关系）不受影响仍逐条记录。
  - result：`output.structured_data.details.images` 列表替换为占位符 `"<已省略 N 张图片，base64 未记录>"`，其余字段不动。
  - 新增 `_strip_images()` 静态方法。
- **验证方式**：
  - `py_compile` 通过；单元级模拟事件流：4 条累计 thinking 合并为 1 条（保留最终完整文本）、状态提示逐条保留、images 变占位符且其他字段（province 等）不受影响，断言全过。
  - 端到端真实诊断（重启 Flask 后 curl /chat）：新日志 21.1KB（原约 345KB，降约 94%）；事件统计 thinking 9 条（8 条状态提示 + 1 条合并后的完整 LLM 思考文本）、result 3 条；雷电 images 字段为 `<已省略 3 张图片，base64 未记录>`；stages 分段计时完整（ai_planning 9.7s / execute_tools 0.6s / compose_report 83s）。
- **遗留问题 / 待优化**：
  - 日志文件仍无轮转/自动清理机制，仅靠单次体积下降（约 0.4MB → 约 21KB）减缓增长；如需彻底治理可再加定期清理策略（用户暂未要求）。
  - 旧日志文件不回溯处理，仅对修改后新产生的诊断生效。

### 2026-08-12

#### 15. 历史关联问题动态化（取消硬编码）

- **修改目的**：历史关联的两个问题由硬编码改为基于本次诊断上下文动态生成——Q1"{线路}历年情况"（线路与诊断一致）；Q2"{province}{voltage}线路{year}年{A}-{B}月情况"（province/voltage 来自 getTripInfoData 接口记录，年月与诊断一致，A=X-1、B=X+1，舍弃跨年：1月→1-2月、12月→11-12月）。
- **涉及范围**：
  - `mcp-services/lightning-service/client.py` → `get_trip_info_data` 返回完整 `TripInfoDataRecord`（原仅返回 tripId），含 province/voltage 字段。
  - `mcp-services/lightning-service/main.py` → `structured_data.details` 增加 province、voltage。
  - `src/application/commands/diagnose.py` → 工具执行完成后从雷电输出提取 province/voltage 幂等写入 `session.fault_context.additional_info`（随会话持久化，仅"清空会话"才删除）。
  - `src/interfaces/web.py` → `/api/history-relation` 改为接收 `{session_id}`，从会话动态拼两个问题；老会话缺字段时仅返回 Q1 + 静态提示节；增加 400/404 错误路径。
  - `web/src/api/http.ts`、`web/src/components/ChatPanel.vue` → 调用带 `activeSessionId`。
- **验证方式**（ULW 4 轮，每轮有验证日志）：
  - 轮1：lightning 单测 6 过；curl /diagnose 返回 province=江西、voltage=±800kV。
  - 轮2：端到端诊断后 sessions.json 的 additional_info 含两键；complete 事件正常。
  - 轮3：curl 实测返回"一、雅湖线历年情况"+"二、江西±800kV线路2025年4-6月情况"，知识库正确识别 ±800kV 并返回统计；400/404 正常；月份边界逻辑验证。
  - 轮4：npm run build 成功。
- **遗留问题 / 待优化**：
  - 缺 province/voltage 的老会话兜底分支（静态提示）未做真实端到端，仅逻辑模拟验证。
  - `additional_info` 中 voltage_level（用户消息解析）与 voltage（接口实测）并存，历史关联以后者为准。

#### 16. 历史关联缓存按会话共享（修改报告后不再重复查询）

- **修改原因**：历史关联缓存原按消息卡片 id 存储，"修改报告"重新生成报告产生新卡片后缓存失效，同一会话内重复查询知识库（约 85 秒/次）；而查询参数来源于会话持久化数据，同会话问题和答案不变。
- **涉及范围**：`web/src/components/ChatPanel.vue` → `historyData`/`historyLoading` 的 key 从消息 id 改为会话 id（`store.activeSessionId`）；展开/收起状态仍按卡片独立。
- **验证方式**：`npm run build` 成功；构建产物含 history-relation 调用与按钮文案。
- **遗留问题 / 待优化**：缓存生命周期为页面存续期间——刷新页面或重进会话后首次点击仍会查询一次（如需跨刷新秒开需后端持久化答案，涉及知识库内容更新失效策略，暂未实施）。

#### 17. 重建 start.sh 一键重启脚本

- **修改原因**：旧 start.sh 为早期版本（dev/docker 模式、Flask 前台运行、不杀旧进程），与当前项目运维方式（nohup 后台 + pid/log 文件）完全不适配，用户授权删除重建。
- **涉及范围**：`start.sh` 删除重建 → `./start.sh` 一条命令完成：按端口（5000、8001-8005）杀旧进程 → 后台启动 5 个 MCP 服务 → 构建前端 → 后台启动 Flask（server.pid/server.log）→ 6 项 /health 健康检查打印。
- **验证方式**：实跑一次，6 个服务全部 [OK]；task.md 启动命令同步更新（`./start.sh dev` → `./start.sh`）。
- **遗留问题 / 待优化**：docker 模式未保留（当前项目无 docker-compose 使用场景）。

#### 18. 风偏/气象权重语义修正（0 × 1.0 = 0）与 DEFAULT_WEIGHTS 同步

- **修改原因**：报告加权章节中风偏/气象原显示"0 × 0 = 0"（权重 0），用户明确语义应为"置信度 0 × 权重 1.0 = 0"，且权重需保留运行时可调整能力。
- **涉及范围**：
  - `skills/comprehensive_diagnosis.md` → 权重 yaml 与调用策略表 Wind/Weather 改为 1.0；注意事项改为"无置信度，按 0 × 配置权重 = 0 计算（默认 1.0），用户调整权重时用调整后的值"——不写死字面量，保留可调整性。
  - `src/core/models.py` → `DEFAULT_WEIGHTS` 同步为 Lightning 1.0 / Icing 0.9 / Wind 1.0 / Weather 1.0（删除已禁用的 Bird，原 Wind 0.8 为旧值）。
- **验证方式**：重启 Flask 后端到端诊断，报告实测：`WindDiagnosisTool: 0 × 1.0 = 0`、`WeatherDiagnosisTool: 0 × 1.0 = 0`，Lightning `0.985 × 1.0 = 0.985` 不变，无鸟害、无 LaTeX。
- **遗留问题 / 待优化**：无。

#### 19. 历史关联后台预取（消息卡片生成即触发查询）

- **修改原因**：原设计点击"历史关联"才触发知识库查询（约 85 秒等待）。用户决策：诊断消息卡片生成后立即后台预取，用户阅读报告期间查询同步进行；点击时若未完成显示"请稍候"，若已完成直接秒开。总时延不变，减少用户感知等待。
- **涉及范围**：`web/src/components/ChatPanel.vue` → 新消息卡片 watch 中，卡片含 report 且 eventType=complete 时自动调用 `loadHistory(sid)`（不展开面板）；复用会话级缓存守卫，"修改报告"产生新卡片时缓存命中不重复查询。
- **验证方式**：`npm run build` 成功，产物含预取逻辑。
- **遗留问题 / 待优化**：无。

---

### 2026-08-11

#### 14. 新增"历史关联"功能（按钮 + 故障知识库联调）

- **修改目的**：诊断完成后，在"查看报告"旁提供"历史关联"入口，通过故障知识库 API 查询两个固定问题并展示，辅助用户关联历史故障。
- **涉及范围**：
  - `web/src/components/ChatPanel.vue` → 诊断卡片"查看报告"旁新增"历史关联"按钮（同款样式），点击展开/收起（展开时文字变"收起"）；展开时调接口并渲染 markdown，含加载提示与失败兜底；会话切换重置状态。
  - `src/interfaces/web.py` → 新增 `POST /api/history-relation`：httpx 串行调用故障知识库 API（`http://localhost:8503/query`，timeout 300s）查询固定两问——"雅湖线历年情况"、"江西800kV线路2025年4-6月雷击情况"；返回 `{sections: [{title, answer}]}`；知识库不可达返回 502 + 错误信息。
  - `web/src/api/http.ts` → 新增 `getHistoryRelation()` 封装。
- **展示格式**：大标题"一、雅湖线历年情况" + 回答；大标题"二、江西800kV线路2025年4-6月雷击情况" + 回答（`##` 标题拼接后走 renderMarkdown，与知识库前端格式一致，含来源文件超链接）。
- **验证方式**：
  - 故障知识库 API 单独实测：`POST /query` 两问均返回正确 markdown（Q1 约 80s / Q2 约 5s）。
  - PLD 接口实测：`POST /api/history-relation` 83.75s 返回 success，两节答案完整。
  - `npm run build` 成功，Flask 正常提供新版前端。
  - coding-protocol 三阶段（Scout→Builder→Verifier）执行完毕，Verifier 判定 Pass。
- **遗留问题 / 待优化**：
  - 知识库地址 `localhost:8503` 硬编码于 web.py（与项目其他本地端点风格一致，未来可抽配置）。
  - 串行调用为用户的明确决策；若未来需提速可改并行（两问并行约 80s → 与单问最耗时者持平）。
  - 故障知识库 API 由另一项目实例开发（FastAPI，端口 8503，`POST /query {question} → {answer}`），本次仅联调验证。

---

### 2026-07-08

#### 1. 替换 LightningDiagnosisTool 为真实雷电故障诊断 MCP 服务

- **修改目的**：将 PLD 中虚拟的雷电诊断工具替换为基于真实数据的雷电故障诊断 MCP 服务。
- **涉及范围**：
  - 新增/替换 `/Users/yfzx/Desktop/Cluade_PLDiagonsis-master/mcp-services/lightning-service/` 目录
  - 服务监听端口：`8001`
- **主要变更**：
  - `main.py` 从返回模拟数据改为调用后端 Mock API，执行 4 模块加权诊断。
  - 保留 FastAPI `/health`、`/diagnose` 接口协议，与 PLD `MCPToolAdapter` 兼容。
  - 输出包含 Markdown 报告、3 张波形图 base64、5 个模块分数、故障类型和综合置信度。
- **验证方式**：
  - `GET /health` 返回正常
  - `POST /diagnose` 对 `雅湖线 2025-05-08` 返回 `雷击-绕击`、`confidence=0.985`、5 条证据、5 个模块分数、3 张图片
  - `pytest test_main.py -v` 全部通过
- **遗留问题 / 待优化**：
  - `config.py` 中 BASE_URL、账号、密码仍硬编码，需改为环境变量配置。
  - `config/tools/lightning.yaml` 中的 `output_schema` 字段仍描述旧虚拟工具输出，建议同步更新。
  - `models.py` 中 `timestamp` 默认值为 naive datetime，建议改为 `datetime.now(timezone.utc)`。
  - 每次请求新建 `httpx.AsyncClient`，未复用连接池，高并发下可优化。

---

### 2026-07-10

#### 2. 新增 MCP 工具调用记录功能

- **修改目的**：为了便于调试 PLD 智能体，记录每次 MCP tool 实际返回的结果。
- **涉及范围**：
  - 修改 `/Users/yfzx/Desktop/Cluade_PLDiagonsis-master/src/infrastructure/adapters/mcp_adapter.py`
- **主要变更**：
  - 在 `MCPToolAdapter.execute()` 成功返回 `ToolOutput` 后，调用新增私有方法 `_save_tool_output()`。
  - `_save_tool_output()` 根据适配器配置的 URL 端口，反向查找 `mcp-services/` 下对应的服务目录名。
  - 以追加模式将调用时间、请求 payload、raw_text、structured_data、metadata 写入 `mcp-services/{service_dir}.md`。
  - 写入失败被 try/except 捕获，不影响主业务流程。
- **验证方式**：
  - `python3 -m py_compile src/infrastructure/adapters/mcp_adapter.py` 通过
  - 直接调用 `MCPToolAdapter.execute()` 成功生成 `mcp-services/lightning-service.md`
  - md 文件中包含完整请求和响应内容
- **未覆盖边界**：
  - HTTP 错误路径（`httpx.HTTPError`）不会保存调用记录。
  - 未测试其余 4 个 MCP 工具。
  - 未测试并发调用时的文件写入安全性。
- **最终判定**：
  - Pass，功能符合需求，可合并到当前工作流。
- **可以优化的点**：
  - 当前时间戳为 UTC，可考虑改为本地时间。
  - `raw_text` 含 Markdown 特殊字符时可能影响 md 文件渲染。
  - md 文件会持续增大，需定期清理或归档。
  - 端口反查依赖 `main.py` 中的硬编码端口模式，若未来通过其他方式配置端口会失效。

---

## 待办事项

- [ ] 将 `lightning-service/config.py` 硬编码配置改为环境变量读取（weather/wind 服务同样适用）。
- [x] ~~更新 `config/tools/lightning.yaml` 的 `output_schema` 以匹配新的 `structured_data`。~~ 已完成（2026-08-12），三个工具 yaml 同步真实字段。
- [ ] 优化 `httpx.AsyncClient` 复用机制。
- [x] ~~将 MCP 调用记录时间戳从 UTC 改为本地时间（可选）。~~ 已完成，改为北京时间。
- [ ] 处理 `raw_text` 中 Markdown 特殊字符，避免渲染异常（可选）。
- [ ] 增加 MCP 调用记录的归档或清理策略（可选）。
- [x] ~~修复或替换 `WeatherDiagnosisTool`（当前通过提示词临时屏蔽）。~~ 已完成，重建为 getWeather 温湿度工具。
- [ ] 优化 `compose_report` 阶段时延（当前约 41 秒，为 LLM 生成报告时间）。
- [ ] 风向参数 wd 映射规则待确认（298.877 与西南风矛盾，需生产端映射逻辑或样本对）。
- [x] ~~覆冰工具接入真实数据源（当前模拟数据 + 季节跳过）。~~ 2026-08-13 用户决策：保持现状（模拟数据 + 季节/温度跳过），目前无该部分数据。
- [x] ~~鸟害工具恢复或正式下线（当前注册表级禁用，8004 进程仍运行）。~~ 2026-08-13 用户决策：保持现状（注册表级禁用），目前无该部分数据。

---

### 2026-07-13

#### 3. 前端端到端验证真实雷电诊断 MCP 服务

- **修改目的**：确认真实雷电诊断 MCP 服务在 PLD 完整链路中被正确调用，报告生成正常。
- **涉及范围**：
  - PLD 前端（Vue 3）+ 后端（Flask）+ `lightning-service`（端口 8001）
- **主要变更**：
  - 启动 PLD 前后端及 `lightning-service`。
  - 在前端输入诊断问题，触发完整诊断流程。
  - 验证 `MCPToolAdapter` 正确调用 `http://localhost:8001/diagnose`。
- **验证方式**：
  - 前端能收到完整 SSE 流并展示诊断结果。
  - `mcp-services/lightning-service.md` 中记录了本次调用。
- **遗留问题 / 待优化**：
  - 验证过程中发现只有 3 个 MCP 服务生成 `.md` 调用记录文件（lightning、wind、bird），属于正常行为：
    - `icing-service` 在诊断计划阶段常被智能体跳过（非当前季节/场景）。
    - `weather-service` 使用自定义浏览器代理适配器，不经过 `MCPToolAdapter`，因此无 `.md` 记录。

---

### 2026-07-17

#### 4. 新增系统日志记录前端输出与全流程时延

- **修改目的**：每次在前端提问时，记录前端完整输出以及 PLD 智能体回答问题的全流程细分时延，便于后续调试功能与性能。
- **涉及范围**：
  - 后端：`src/infrastructure/diagnosis_logger.py`（新增）
  - 后端：`src/core/models.py`（`ExecutionContext` 新增 `diagnosis_logger` 字段）
  - 后端：`src/application/commands/diagnose.py`（各阶段计时）
  - 后端：`src/domain/tool_executor.py`（单工具耗时）
  - 后端：`src/interfaces/web.py`（SSE 事件记录、前端日志合并接口 `/api/log/frontend`）
  - 前端：`web/src/api/http.ts`（新增 `sendFrontendLog`）
  - 前端：`web/src/stores/sessionStore.ts`（捕获时间线、发送前端日志）
- **主要变更**：
  - 新增 `DiagnosisLogger` 类，按日期分目录、按会话写入 JSON 日志。
  - `DiagnoseCommand` 对 10 个主要阶段记录开始/结束时间。
  - `ToolExecutor` 记录每个工具的调用耗时与成败。
  - `_chat_stream` 将每个 SSE 事件写入日志。
  - 前端在 SSE 流结束后，自动将完整 `messages` 列表和时间线 POST 到 `/api/log/frontend`。
  - 后端合并前端数据，最终文件包含 `frontend_output`、`frontend_timeline`、`stages`、`events`、`tool_timings`。
- **验证方式**：
  - `python3 -m py_compile` 通过相关后端文件。
  - 使用 Playwright 真实浏览器访问 `http://localhost:5173/` 发送诊断问题。
  - 自动生成日志文件，包含 2 条前端消息、完整时间线、10 个阶段、6 个工具耗时。
- **遗留问题 / 待优化**：
  - `thinking` 事件重复生成较多，日志文件可能偏大，可考虑采样或压缩。
  - 当前日志保存在本地 `logs/diagnosis/`，长期运行需考虑归档策略。

### 2026-07-17

#### 5. 屏蔽 WeatherDiagnosisTool 以优化诊断时延

- **修改目的**：`WeatherDiagnosisTool` 通过浏览器代理抓取百度天气，经常超时，单次诊断耗时 109 秒，严重拖慢整体响应。
- **涉及范围**：
  - 修改 `skills/comprehensive_diagnosis.md`
- **主要变更**：
  - 在工具调用策略表格中增加 `WeatherDiagnosisTool` 行，标记为 `DO NOT call`。
  - 在注意事项中增加明确说明：`Skip WeatherDiagnosisTool completely - it is currently disabled due to browser automation timeout`。
- **验证方式**：
  - 重启服务后发送诊断请求，日志显示只调用 3 个工具（Lightning、Wind、Bird）。
  - `execute_tools` 阶段从 109 秒降到约 0.4 秒。
  - 前端总诊断耗时从约 2.5 分钟降到约 52 秒。
- **遗留问题 / 待优化**：
  - 未来修复天气工具后，需要删除这两行提示词说明以恢复调用。
  - 当前 `compose_report` 阶段成为新瓶颈（约 41 秒），这是 LLM 生成报告的时间。

---

#### 6. MCP 调用记录改为覆盖模式并使用北京时间

- **修改目的**：
  1. 每次 MCP 调用记录只保留最新一次，便于查阅。
  2. 将调用时间从 UTC 改为北京时间。
- **涉及范围**：
  - 修改 `src/infrastructure/adapters/mcp_adapter.py`
- **主要变更**：
  - `_save_tool_output` 写入模式从追加 `"a"` 改为覆盖 `"w"`。
  - 时间戳从 `datetime.now(timezone.utc)` 改为 `datetime.now(timezone(timedelta(hours=8)))`。
  - 文档字符串同步更新为"覆盖写入"。
- **验证方式**：
  - `python3 -m py_compile src/infrastructure/adapters/mcp_adapter.py` 通过。
  - 调用后 `mcp-services/lightning-service.md` 从 698,855 字节降到约 100 KB。
  - `grep -c '## 调用记录' mcp-services/lightning-service.md` = 1，确认只有一条记录。
  - 时间戳显示为 `2026-07-17 15:21:21 UTC+08:00`。
- **遗留问题 / 待优化**：
  - 覆盖模式会丢失历史记录，如需恢复追加模式，需改回 `"a"`。
  - 高并发场景下多个请求可能互相覆盖，当前单用户场景可接受。

---

### 2026-08-10

#### 7. 修复报告中 LaTeX 乱码问题（清洗器 + Prompt 引导双层防御）

- **修改目的**：LLM 生成诊断报告时自发使用 LaTeX `$...$` 语法（如 `$0.985 \times 1.0$`、`$\text{kA}$`），前端 marked-katex-extension 对紧贴中文字符的 `$` 无法渲染，用户看到原始 LaTeX 源码（乱码）。
- **根因**：已逐环节排除——MCP 工具输出为纯文本、prompt/skill 从未要求 LaTeX、`$` 仅出现在 LLM 最终报告中。系 LLM 训练习惯，无法通过 prompt 根除，必须代码层确定性清洗。
- **涉及范围**：
  - 新增 `src/domain/report_sanitizer.py`（清洗模块，纯正则，无新依赖）
  - 修改 `src/domain/report_composer.py`（`_format_response()` 接入清洗 + 格式要求加第 6 条）
  - 修改 `src/application/commands/modify_report.py`（LLM 修改报告路径接入清洗）
  - 修改 `skills/comprehensive_diagnosis.md`（注意事项加"禁止 LaTeX，使用纯文本符号"）
  - 新增 `tests/unit/test_report_sanitizer.py`（36 个测试，用例来自历史真实样本）
- **主要变更**：
  - `sanitize_report()` 将 `$...$` 公式转为纯文本：`\times`→×、`\le`→≤、`\ge`→≥、`^\circ`→°、`\text{x}`→x、`\pm`→±、`\%`→%，未知命令兜底去反斜杠。
  - 覆盖全部 2 个报告写入点（`diagnose.py` 初次诊断、`modify_report.py` 修改报告）。
  - 清洗函数幂等：无 LaTeX 文本原样返回，重复执行不破相。
- **验证方式**：
  - 36 个单元测试全部通过（含 mock LLM 的 `compose()` 接入点集成测试）。
  - 7 份历史含 LaTeX 报告清洗后 0 残留。
  - 端到端冒烟：通过运行中服务发起真实诊断（LLM 在线），最终报告 1530 字符，`$` 与 LaTeX 命令均为 0 处，数学表达为纯文本（`0.985 × 1.0 = 0.985`）；持久化到 `sessions.json` 的报告同样干净。
  - 存量数据检查：`sessions.json` 当前 0 个会话，无历史脏报告需清洗。
- **遗留问题 / 待优化**：
  - 环境缺 `pytest-asyncio`，106 个存量异步测试无法运行（基线问题，与本次无关）。
  - 极端场景未覆盖：报告中两个独立 `$`（如货币）会被误认为一个公式；代码块内 `$...$` 也会被转换。历史数据无此样本，风险低。
  - 前端仍保留 KaTeX 渲染配置（`web/src/utils/markdown.ts`），作为第三层兜底，未做改动。

---

### 2026-08-10

#### 8. 雷电诊断工具删除模块四（微气象），权重重新分配

- **修改目的**：getWeather 接口的 4 个参数（风速 ws、风向 wd、温度 tmp、湿度 hum）后续将分别复用于风偏诊断和气象诊断 MCP 工具，雷电诊断不再消费微气象数据。删除模块四后，其 0.10 权重分配给模块二（0.15→0.20）和模块 3.2（0.30→0.35），权重合计保持 1.00。
- **涉及范围**：
  - `mcp-services/lightning-service/diagnosis_engine.py`（删除 `analyze_module4()`、`build_report()` 去掉 weather 参数、权重与文案同步、`_WEIGHT_LABELS` 补 0.20/0.35）
  - `mcp-services/lightning-service/client.py`（`fetch_all_diagnosis_data()` 改为 3 路并行，不再拉取 getWeather；保留 `get_weather()` 方法供后续复用）
  - `mcp-services/lightning-service/main.py`（解包 3 个数据源）
  - `test_main.py` 无需改动（不涉及模块四）
- **主要变更**：
  - 模块权重：模块一 0.30、模块二 0.20、模块 3.1 0.15、模块 3.2 0.35。
  - 报告删除"模块四：微气象"章节，综合结论改为"基于 4 个证据条目"。
  - 每次诊断减少 1 次 getWeather HTTP 请求。
  - 微气象评分逻辑（湿度 >70%→1.0 / ≥40%→0.8 / 否则→0）已留存至持久记忆，供风偏/气象工具复用。
- **验证方式**：
  - `py_compile` 通过；`pytest test_main.py` 6 个测试全部通过。
  - 实测 `POST /diagnose`（雅湖线 2025-05-08）：报告无模块四；module_scores 为 4 条，权重 0.30/0.20/0.15/0.35 合计 1.0；综合置信度 0.985（=0.95×0.30+1.0×0.20+1.0×0.15+1.0×0.35）；故障类型仍为"雷击-绕击"。
- **遗留问题 / 待优化**：
  - 波形图（3 张 base64 PNG）已到达前端但被 sessionStore 丢弃，用户决策暂不修改，后续有需求再加。
  - 前端 PLD 报告由 LLM 综合生成，新权重结构不影响其输出（权重 1.0 为 PLD 层配置，与本服务内部模块权重无关）。

---

### 2026-08-10

#### 9. 重建气象诊断 MCP 工具（getWeather 温湿度 + 湿度描述句，不参与加权）

- **修改目的**：旧 WeatherDiagnosisTool 使用浏览器代理抓取百度天气，超时严重（单次约 109 秒）且已被提示词屏蔽。改用特高压 getWeather 接口的温度（tmp）和湿度（hum）两个参数，输出纯语言描述。
- **涉及范围**：
  - `mcp-services/weather-service/` 整目录重建（9 个文件，复用 lightning-service 的 client/engine 架构，端口 8005；旧代码用户已备份为 `weather-service（旧版）`）
  - `config/tools/weather.yaml`：adapter 从 `custom`（BrowserAgentAdapter）改为 `mcp`（http://localhost:8005）
  - `skills/comprehensive_diagnosis.md`：解除屏蔽，权重标记为 0（参考信息，不参与加权）
- **主要变更**：
  - 输出仅三部分：故障时刻温度、湿度、湿度级别描述句（>70%→极为潮湿有利于雷暴；≥40%→较为潮湿符合雷暴条件；否则→干燥不符合，文案逐字沿用雷电原模块四）。
  - `structured_data` 为 `temperature_c`/`humidity_pct`/`humidity_description`/`details`，**不含** confidence/fault_type，对 PLD 加权诊断无贡献。
  - 保留 `os.environ.get("PORT", 8005)` 模式，MCPToolAdapter 自动生成覆盖式 `weather-service.md` 调用记录（北京时间）。
- **验证方式**：
  - 11 个单元测试全部通过（含湿度分档边界 70/40、报告无置信度/权重字样断言）。
  - 实测 `POST /diagnose`（雅湖线 2025-05-08）：温度 26.130 °C、湿度 80.161%、描述句正确。
  - 端到端诊断：工具被规划器调用，报告含湿度描述，加权计算中无 Weather 项。
- **遗留问题 / 待优化**：
  - 无。

---

### 2026-08-10

#### 10. 重建风偏诊断 MCP 工具（getWeather 风速 + 阈值描述句，不参与加权）

- **修改目的**：风偏工具从模拟数据改为 getWeather 接口的风速（ws）参数，输出纯语言描述。风向（wd）参数弃用：实测 wd=298.877，用户确认实际为西南风，但该值在任何标准风向编码（8/16 方位、去向/来向、百分度、密位）下均映射为西~西北，与西南矛盾，单样本无法反推生产端映射规则。
- **涉及范围**：
  - `mcp-services/wind-service/` 整目录重建（旧代码已备份为 `wind-service（旧版）`，端口 8003）
  - `config/tools/wind.yaml`：描述与 output_schema 同步
  - `skills/comprehensive_diagnosis.md`：权重 0.8 → 0（参考信息，不参与加权）+ 注意事项说明
- **主要变更**：
  - 判定规则：ws ≤ 10 m/s →"风速小于设计风速的60%，无风偏故障风险"；ws > 10 m/s →"风速大于设计风速的60%，请关注是否存在风偏故障"。
  - `structured_data` 为 `wind_speed_ms`/`wind_description`/`details`，不含 confidence/fault_type。
- **验证方式**：
  - 11 个单元测试全部通过（含阈值边界 10.0/10.001）。
  - 实测 `POST /diagnose`（雅湖线 2025-05-08）：风速 3.408 m/s，命中"无风偏故障风险"档。
- **遗留问题 / 待优化**：
  - wd 风向映射规则未确定。若后续需要风向展示，须先获取生产平台前端映射逻辑或多组（wd 值 → 实际方位）样本对。

---

### 2026-08-10

#### 11. 鸟害工具注册表级禁用（诊断与前端均不可见）

- **修改目的**：故障诊断不再使用鸟害工具，且前端"诊断工具"栏不显示"鸟害监测"。
- **涉及范围**：
  - `config/tools/bird.yaml`：新增 `enabled: false`
  - `src/infrastructure/adapters/registry.py`：`_load_tool_config()` 检查 enabled 标记，为 false 时跳过注册
  - `skills/comprehensive_diagnosis.md`：删除 BirdDamageDiagnosisTool 工具表行，注意事项说明已禁用
- **主要变更**：注册表级硬约束——LLM 规划器看不到该工具（比提示词屏蔽更彻底），`/api/tools` 不返回，前端面板自动隐藏。
- **验证方式**：
  - 注册表日志：`工具已禁用，跳过注册: BirdDamageDiagnosisTool (bird.yaml)`，`已加载 4 个诊断工具`。
  - `GET /api/tools` 实测仅返回雷电/覆冰/风偏/气象 4 个工具。
- **遗留问题 / 待优化**：
  - 8004 bird-service 进程保留运行但不会被调用；恢复方法为改回 `enabled: true` 并重启后端。

---

### 2026-08-10

#### 12. Mock 多账号修复并行调用 token 竞争

- **根因**：Mock API 单账号单 token——每次 login 作废同账号此前的 token。气象/风偏工具改造后与雷电共用 yfzx 账号，并行诊断时 login 互相作废，导致雷电工具稳定返回 500（日志：`认证已过期，请重新登录`，并发 curl 实测稳定复现）。
- **涉及范围**：
  - `mcp-services/lightning-service/config.py` → 账号 YFZX-1
  - `mcp-services/weather-service/config.py` → 账号 YFZX-2
  - `mcp-services/wind-service/config.py` → 账号 YFZX-3
  - （密码不变；原 yfzx 账号已弃用；Mock 平台侧多账号支持由用户完成）
- **验证方式**：
  - 并发直连三服务：修改前 lightning 稳定 500，修改后三个全部 200 且内容正确。
  - 端到端并行诊断：三工具全部成功（雷电 雷击-绕击 0.985、气象/风偏描述句正常）；摘要 雷击-绕击 0.98；加权计算仅 `0.985 × 1.0 = 0.985`；报告无 LaTeX 残留；三份 `.md` 调用记录生成（含首次生成的 `wind-service.md`）。
  - 串行时延实测（决策依据）：三工具串行总和 0.219s vs 并行 0.158s，差异约 0.06 秒，占整次诊断（约 52 秒）约 0.1%。
- **遗留问题 / 待优化**：
  - 曾实施"强制串行执行"过渡方案，后按用户决策撤回（`tool_executor.py` 恢复原 parallel 分组逻辑，git diff 为空确认一致）。
  - 覆冰（icing）工具仍为模拟数据，未接入真实数据源；保留"夏季/温度>5°C 跳过"的提示词级软约束。

---

#### 13. 工具展示名与加权章节措辞调整

- **修改原因**：用户要求（1）前端"诊断工具"栏"气象诊断"改为"微气象诊断"；（2）报告"加权置信度计算"章节中风偏直接写 0、不出现鸟害工具。
- **涉及范围**：
  - `config/tools/weather.yaml` → `display_name: "微气象诊断"`
  - `skills/comprehensive_diagnosis.md` → 权重 yaml 块改为 Lightning 1.0 / Icing 0.9 / Wind 0 / Weather 0（删除 BirdDamageDiagnosisTool 行）；工具调用策略表风偏/气象权重列均改为 0；注意事项风偏/气象条目均改为"在加权置信度计算章节直接写 0 × 0 = 0"；删除鸟害禁用说明条目。
- **验证方式**：
  - `GET /api/tools` 返回"微气象诊断"。
  - 端到端诊断报告加权章节实测：`LightningDiagnosisTool: 0.985 × 1.0 = 0.985`、`WindDiagnosisTool: 0 × 0 = 0`、`WeatherDiagnosisTool: 0 × 0 = 0`，无"参考"措辞，无鸟害出现，无 LaTeX 残留。
- **遗留问题 / 待优化**：
  - `SkillLoader` 有进程内缓存，修改 skill 文件后必须重启后端才生效（本次曾因此出现首轮验证仍是旧措辞的情况）。

---

## 备注

- 后续对项目的任何修改，请按时间倒序追加到本文件。
